import { getConnection } from "typeorm";
import "./connection";

import { Post } from "../src/model/post";
import { User } from "../src/model/user";

describe("Post 데이터베이스  테스트", () => {
  test("Post 생성", async () => {
    // let author = new User();
    // const createdPost = await creatPost("쉬운코딩", "글의 내용입니다.", author);
  });
});
